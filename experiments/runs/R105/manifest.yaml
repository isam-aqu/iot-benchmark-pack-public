run_id: R105
title: Wi-Fi rerun with improved stability (best run)
date: 2026-03-30

git:
  branch: testbed-dev
  commit: null
  tag: null

versions:
  firmware: "0.1.1"
  ha_config: "0.1.1"
  analysis: "0.1.1"

experiment:
  plan_id: W1
  workload: W1
  protocol: wifi
  topology: near
  interference: quiet
  load_level: single
  node_count: 1
  interval_ms: 3000-5000
  trigger: button_press
  normalization_method: none
  scenario_label: "W1_wifi_near_quiet"
  run_label: "W1_wifi_near_quiet_v5"

hardware:
  controller: "Raspberry Pi 4 running Home Assistant OS"
  logger_host: "external laptop"
  esp32_node: "ESP32 dev board"
  node_id: "wifi01"

software:
  home_assistant: "versioned via experiments/version_info.yaml"
  mqtt_broker: "Mosquitto"

timing:
  timestamp_source: "ESP32 RTT measurement"
  ntp_enabled: false
  timezone: "Asia/Hebron"

artifacts:
  raw:
    - raw/W1_wifi_near_quiet_v5_mqtt_events.csv
  derived:
    - derived/W1_wifi_near_quiet_v5_events_only.csv
    - derived/W1_wifi_near_quiet_v5_wifi_rtt_full.csv
    - derived/W1_wifi_near_quiet_v5_wifi_rtt_core.csv

result_summary:
  status: complete
  key_observation: "Best Wi-Fi run so far with only 9% outliers and stable core latency, but rare extreme spikes still present."
  anomalies:
    - "9 out of 100 RTT samples exceeded 200 ms."
    - "Maximum RTT reached 1532.037 ms."

dataset:
  total_samples: 100
  core_samples: 91
  outlier_handling: "RTT > 200 ms excluded from core dataset"
  filtering_range_ms: [0, 200]

notes: >
  This run demonstrates that Wi-Fi can achieve stable latency under the current
  setup, with the lowest observed outlier rate (9%). However, rare extreme spikes
  above 1 second persist, indicating a systemic issue rather than random noise.
